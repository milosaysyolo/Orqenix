// SPDX-License-Identifier: Apache-2.0// verify-phase-8.mjs// Full verification pipeline for Phase 8 CORE.// Steps: install GåÆ native-bindings GåÆ typecheck GåÆ lint GåÆ test GåÆ build GåÆ stub-wiring//// Run://   node scripts/verify/verify-phase-8.mjs                  # full pipeline//   node scripts/verify/verify-phase-8.mjs --skip-install   # skip install step//   node scripts/verify/verify-phase-8.mjs --no-build       # skip build step//   node scripts/verify/verify-phase-8.mjs --phase-8-only   # test only Phase 8 packagesimport { spawnSync } from 'node:child_process';import { existsSync, writeFileSync, mkdirSync } from 'node:fs';import { join } from 'node:path';const ROOT = process.cwd();const args = process.argv.slice(2);const isWin = process.platform === 'win32';// --skip-X / --no-X flagsconst skipInstall = args.includes('--skip-install');const skipTypecheck = args.includes('--skip-typecheck');const skipLint = args.includes('--skip-lint');const noBuild = args.includes('--no-build');const phase8Only = args.includes('--phase-8-only');const writeReportFlag = args.includes('--report');function shouldRun(label) {  if (label === 'install' && skipInstall) return false;  if (label === 'typecheck' && skipTypecheck) return false;  if (label === 'lint' && skipLint) return false;  if (label === 'build' && noBuild) return false;  return true;}function section(title) {  console.log(`\n${'='.repeat(64)}`);  console.log(`  ${title}`);  console.log(`${'='.repeat(64)}`);}function log(msg) {  console.log(`  ${msg}`);}const steps = [];function writeReport() {  const allOk = steps.every((s) => s.ok);  const report = {    pipeline: 'Phase 8 CORE Verification',    timestamp: new Date().toISOString(),    platform: { node: process.version, os: process.platform, arch: process.arch },    summary: { total: steps.length, passed: steps.filter((s) => s.ok).length, failed: steps.filter((s) => !s.ok).length },    steps,    verdict: allOk ? 'PASS' : 'FAIL',  };  writeFileSync('verify-report.json', JSON.stringify(report, null, 2));  log(`Report: verify-report.json`);}// GöÇGöÇ Step 1: INSTALL GöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇif (shouldRun('install')) {  section('STEP 1: Install dependencies');  const r = spawnSync('pnpm', ['install'], {    cwd: ROOT, stdio: 'inherit', encoding: 'utf-8', shell: isWin,  });  steps.push({ label: 'install', ok: r.status === 0, fatal: true });  if (r.status !== 0) {    log('G£ù Install failed');    writeReport();    process.exit(1);  }  log('G£ô Dependencies installed');}// GöÇGöÇ Step 1.5: NATIVE BINDINGS GöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇif (shouldRun('native')) {  section('STEP 1.5: Native bindings (better-sqlite3, esbuild, @swc/core)');  const r = spawnSync('node', ['scripts/verify/check-native-bindings.mjs', '--auto-rebuild'], {    cwd: ROOT, stdio: 'inherit', encoding: 'utf-8',  });  steps.push({ label: 'native-bindings', ok: r.status === 0, fatal: true });  if (r.status !== 0) {    log('G£ù Native bindings cannot load. Run `pnpm run rebuild:native` manually, then re-verify.');    writeReport();    process.exit(1);  }}// GöÇGöÇ Step 2: TYPECHECK GöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇif (shouldRun('typecheck')) {  section('STEP 2: Typecheck');  const r = spawnSync('pnpm', ['-r', 'run', 'typecheck'], {    cwd: ROOT, stdio: 'inherit', encoding: 'utf-8', shell: isWin,    timeout: 300000,  });  steps.push({ label: 'typecheck', ok: r.status === 0, fatal: true });  if (r.status !== 0) {    log('G£ù Typecheck failed');    writeReport();    process.exit(1);  }  log('G£ô Typecheck passed');}// GöÇGöÇ Step 3: LINT GöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇif (shouldRun('lint')) {  section('STEP 3: Lint');  const r = spawnSync('pnpm', ['-r', '--filter="./packages/*"', 'lint'], {    cwd: ROOT, stdio: 'inherit', encoding: 'utf-8', shell: isWin,    timeout: 120000,  });  steps.push({ label: 'lint', ok: r.status === 0, fatal: false });  if (r.status !== 0) {    log('G£ù Lint failed (non-fatal for Phase 8 gate)');  } else {    log('G£ô Lint passed');  }}// GöÇGöÇ Step 5: TEST (scoped to Phase 8 by default) GöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇif (shouldRun('test')) {  section('STEP 5: Test (scoped to Phase 8 by default)');  const testArgs = phase8Only ? ['--phase-8-only', '--report'] : ['--all', '--report'];  const r = spawnSync('node', ['scripts/verify/test-gate.mjs', ...testArgs], {    cwd: ROOT, stdio: 'inherit', encoding: 'utf-8',  });  steps.push({    label: phase8Only ? 'test (phase-8 only)' : 'test (all)',    ok: r.status === 0,    fatal: phase8Only, // legacy fails are non-fatal  });  if (r.status !== 0) {    log(phase8Only ? 'G£ù Test gate FAILED (blocking)' : 'G£ù Some tests failed (non-blocking for legacy)');  }}// GöÇGöÇ Step 6: BUILD GöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇif (shouldRun('build')) {  section('STEP 6: Build');  const r = spawnSync('pnpm', ['-r', 'run', 'build'], {    cwd: ROOT, stdio: 'inherit', encoding: 'utf-8', shell: isWin,    timeout: 600000,  });  steps.push({ label: 'build', ok: r.status === 0, fatal: true });  if (r.status !== 0) {    log('G£ù Build failed');    writeReport();    process.exit(1);  }  log('G£ô Build passed');}// GöÇGöÇ Step 7: STUB WIRING GöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇif (shouldRun('stubs')) {  section('STEP 7: Stub-wiring check');  const script = join(ROOT, 'scripts/verify/check-stub-wiring.mjs');  if (existsSync(script)) {    const r = spawnSync('node', ['scripts/verify/check-stub-wiring.mjs'], {      cwd: ROOT, stdio: 'inherit', encoding: 'utf-8',    });    steps.push({ label: 'stub-wiring', ok: r.status === 0, fatal: false });    if (r.status !== 0) {      log('G£ù Stub wiring check failed (non-fatal)');    } else {      log('G£ô Stub wiring check passed');    }  } else {    log('GÅ¡  Stub wiring script not found (skipped)');    steps.push({ label: 'stub-wiring', ok: true, fatal: false, skipped: true });  }}// GöÇGöÇ SUMMARY GöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇGöÇconsole.log(`\n${'GòÉ'.repeat(64)}`);console.log('  VERIFICATION SUMMARY');console.log(`${'GòÉ'.repeat(64)}`);let allOk = true;for (const s of steps) {  const icon = s.ok ? 'G£à' : 'G¥î';  console.log(`  ${icon} ${s.label}`);  if (!s.ok) allOk = false;}console.log(`\n  Verdict: ${allOk ? 'G£à PASS' : 'G¥î FAIL'}`);console.log(`  Steps: ${steps.filter(s => s.ok).length}/${steps.length} passed`);if (writeReportFlag) {  writeReport();}process.exit(allOk ? 0 : 1);