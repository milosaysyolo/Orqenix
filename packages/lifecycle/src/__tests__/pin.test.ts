import { describe, it, expect } from "vitest";

describe("pin", () => {
  it("should add pin", () => {
    expect(true).toBe(true);
  });

  it("should remove pin", () => {
    expect(true).toBe(true);
  });

  it("should list pins", () => {
    expect(true).toBe(true);
  });
});
